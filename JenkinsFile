pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          curl --version
          node --version
          npm --version
          python --version
          pip --version
        '''
      }
    }
  }
}
